# Signing keys — NEVER commit. Back these up securely elsewhere.
*.keystore
*.jks
android.keystore

# Bubblewrap / Gradle build output
app-release-bundle.aab
app-release-signed.apk
app-release-unsigned-aligned.apk
build/
.gradle/
node_modules/

# Bubblewrap local config that can embed key paths/passwords
bubblewrap.json
